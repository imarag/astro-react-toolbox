---
import { titleClass } from "@utils/componentClasses/titleClass";
const { variant, class: className, ...rest } = Astro.props;
---

<h1 class={titleClass({ variant, className })} {...rest}><slot /></h1>
